{% extends 'layouts/admin.twig' %}

{% import 'macros/form.twig' as form %}

{% block content %}
    <section class="content-header">
        <h1>{{ lang('GROUP_CREATION') }}</h1>
        <ol class="breadcrumb">
            <li><a href="{{ route('admin/admin_index') }}"><i class="fa fa-dashboard"></i> {{ lang('HOME') }}</a></li>
            <li>{{ lang('GROUPS') }}</li>
            <li class="active">{{ lang('GROUP_CREATION') }}</li>
        </ol>
    </section>
    <section class="content">
        <div class="box">
            <div class="box-header with-border">
                <h3 class="box-title">{{ lang('CREATE_GROUP') }}</h3>
            </div>
            <div class="box-body">
                {{ form_open() }}
                <div class="col-md-12">
                    {{ form.input('name', lang('GROUP_NAME'), this.input.post('name'), {placeholder: lang('GROUP_NAME_EXAMPLE'), required: true}) }}
                </div>
                <div class="col-md-12">
                    {{ form.input('color', lang('GROUP_COLOR'), this.input.post('color'), {type: 'color', required: true}) }}
                </div>
                <div class="col-md-12">
                    {{ form.selectMultiple('permissions', lang('GROUP_PERMISSIONS'), permissions, this.input.post('permissions'), {height: 300}) }}
                </div>

                <div class="col-md-12">
                    {{ form.submit(lang('CREATE'), null, {btnType: 'success'}) }}
                </div>
                {{ form_close() }}
            </div>
        </div>
    </section>
{% endblock %}